{
  "name": "@skyphusion/security-audit",
  "version": "0.1.0",
  "description": "Adversarial LLM security audits for GitHub repos via Cloudflare Workers AI",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/skyphusion-labs/security-audit.git"
  },
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "node --check adversarial-audit.mjs && node --check redact.mjs && bash -n post-pr-comment.sh"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "vitest": "^4.1.10"
  }
}
